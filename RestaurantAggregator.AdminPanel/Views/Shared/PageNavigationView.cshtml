@using Microsoft.AspNetCore.Mvc.TagHelpers
@model RestaurantAggregator.Common.Dtos.PageInfo

<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">

<form>
    @{
        var startIndex = Math.Max(1, Model.Current - 2);
        var endIndex = Math.Min(Model.Size, Model.Current + 2);

        if (startIndex > 1)
        {
            <button type="submit" asp-action="Index" name="page" value="1" class="material-symbols-outlined">keyboard_double_arrow_left</button>
        }

        for (var i = startIndex; i <= endIndex; i++)
        {
            if (i != Model.Current)
            {
                <button type="submit" asp-action="Index" name="page" value="@i">@i</button>
            }
            else
            {
                <button type="submit" asp-action="Index" name="page" value="@i" style="background: white">@i</button>
            }
        }

        if (endIndex < Model.Size)
        {
            <button type="submit" asp-action="Index" name="page" value="@Model.Size" class="material-symbols-outlined">keyboard_double_arrow_right</button>
        }
    }
</form>